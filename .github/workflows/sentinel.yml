name: 01. Sentinel Scout (Frontline Scan)

on:
  schedule:
    - cron: '0 0,12 * * *' # 北京时间 8:00 和 20:00
  workflow_dispatch:

jobs:
  scout_mission:
    runs-on: ubuntu-latest
    permissions:
      contents: write # 必须赋予写权限以提交数据

    steps:
      - name: 📍 Checkout Frontline
        uses: actions/checkout@v4

      - name: 🔧 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: 📦 Install Dependencies
        run: npm install @octokit/rest --no-fund

      # Phase 1: 执行侦察 (生成数据到 data/tech)
      - name: 📡 Run Sentinel Scan
        run: node sentinel.js
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO_OWNER: ${{ github.repository_owner }}
          REPO_NAME: ${{ github.event.repository.name }}

      # Phase 2: 暂存情报 (提交到当前仓库)
      - name: 💾 Commit Intel to Frontline
        run: |
          git config --global user.name 'Sentinel Scout'
          git config --global user.email 'scout@architect.alpha'
          
          # 检查是否有新数据生成
          if [[ -n $(git status --porcelain data/) ]]; then
            git add data/
            git commit -m "Intel: New tech signals captured [$(date +%F)]"
            git push
          else
            console.log("No new intel found, skipping commit.")
          fi
