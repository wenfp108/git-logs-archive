name: GitHub Sentinel (Strategy Scan)

on:
  schedule:
    - cron: '0 0,12 * * *' 
  workflow_dispatch:

permissions:
  contents: write

jobs:
  scout_and_harvest:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Frontline Repo
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.MY_PAT }} # 使用 PAT 以便后续推送清理结果

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Dependencies
        run: npm install @octokit/rest --no-fund

      # 🎯 Phase 1: 哨兵执行侦察任务 (生成数据)
      - name: 📡 Run Sentinel Scan
        run: node sentinel.js
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO_OWNER: ${{ github.repository_owner }}
          REPO_NAME: ${{ github.event.repository.name }}

      # 🏦 Phase 2: 准备收割环境
      - name: Checkout Central Bank
        uses: actions/checkout@v4
        with:
          repository: wenfp108/Central-Bank
          token: ${{ secrets.MY_PAT }}
          path: central_bank

      - name: 🏦 Execute Archive Logic
        run: node scripts/archive-git-to-bank.js

      # 📤 Phase 3: 推送至中央银行
      - name: Push to Central Bank
        run: |
          cd central_bank
          git config --global user.name 'Architect Bot'
          git config --global user.email 'bot@architect.alpha'
          git add .
          git commit -m "Vault Update: GitHub Tech Sentinel $(date +%F)" || exit 0
          git push

      # 🧹 Phase 4: 清理前线并推送
      - name: Sync & Cleanup Frontline
        run: |
          # 核心修复：彻底删除子仓库目录，防止干扰主仓库 Git 状态
          rm -rf central_bank 
          
          git config --global user.name 'Architect Bot'
          git config --global user.email 'bot@architect.alpha'
          # 同步 data/ 目录下的物理删除操作
          git add data/
          git commit -m "Cleanup: Data flushed to Central Bank [skip ci]" || exit 0
          git push
